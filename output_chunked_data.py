from chunked_data_handler.ChunkedDataHandler import ChunkedDataHandler

# Instantiate the Chunked Data Handler
chunked_handler = ChunkedDataHandler()

# Output chunked data to the output folder
chunked_handler.output_chunked_data()
